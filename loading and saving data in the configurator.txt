


							{	Examples of how to read file values to the form.
                             1: UoDataFileRoot.Text := ParamVal;
                              2: RetainCleartextPasswords.Checked := Numb2Dec(ParamVal,2) = 1;
                              // To convert numbers in the file to drop down list entries in the GUI:
                              3: Begin
                                    ScratchInt := Numb2Dec(ParamVal, 10);
                                    MinCmdlevelToLogin.ItemIndex := ScratchInt;
                                 End;
                              // To take descriptions from the GUI and insert them in the file use this:
                             48: Begin
  	 			            		StList := MiniDumpType.Items;
   			                  		ScratchInt := StList.IndexOf(ParamVal);
        	                        MiniDumpType.ItemIndex := ScratchInt;
                                 End;
								 
								 
								To save to the cfg file.
								To save a dropdown box which needs to be converted to an index number
								2: ParamVal := AssertionFailureAction.Items[AssertionFailureAction.ItemIndex];
								
								To save a checkbox
								4: ParamVal := IntToStr(StrToInt(BoolToStr(CoreGuildMessages.Checked)) * -1);
								
								To save a text box
								5: ParamVal := CoreHandledTags.Text;


